export default (type : string) => {
    if (type === 'dog') {
      return 'Encontrei um cachorrinho'
    } if (type === 'cat') {
      return 'Encontrei um gatinho'
    } else {
      return 'Encontrei um amiguinho'
    }
  }
